# Runtime dependencies for the FastAPI service ONLY (used for deployment).
# This is the subset of requirements.txt that app/ actually imports — it
# drops the notebook/visualization stack (rasterio, matplotlib, contextily,
# folium, leafmap, ipywidgets, jupyter) and test tools, for a faster and
# more reliable build on hosted platforms (e.g. Render).
# Local dev / notebooks / CI still use the full requirements.txt.

# --- API framework ---
fastapi==0.136.0
uvicorn==0.45.0

# --- Database / ORM ---
SQLAlchemy==2.0.49
psycopg2-binary==2.9.11

# --- Geospatial (vector) — geopandas pulls pyproj transitively ---
geopandas==1.1.2
shapely==2.1.2
pyogrio==0.12.1

# --- Data analysis ---
pandas==2.3.3
numpy==2.4.2

# --- Config ---
python-dotenv==1.2.2
